$this is an example program
let
	MAX ~ 10,
	num: Integer
in
	{
		getint(var num),
		if num > 0 then
			if num < MAX then
				while num > 0 do
					{
						putint(num), puteol(),
						num becomes num-1
					}
			else nothing
		else nothing
	}
